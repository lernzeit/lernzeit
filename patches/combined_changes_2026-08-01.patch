--- a/index.html
+++ b/index.html
@@ -10,7 +10,7 @@
     <meta name="google-play-app" content="app-id=de.lernzeit.app" />
     
     <!-- Content Security Policy -->
-    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://ai.gateway.lovable.dev https://accounts.google.com https://api.stripe.com; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; frame-src https://accounts.google.com https://js.stripe.com https://hooks.stripe.com;">
+    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob:; worker-src 'self' blob:; connect-src 'self' blob: https://*.supabase.co wss://*.supabase.co https://ai.gateway.lovable.dev https://accounts.google.com https://api.stripe.com https://api.revenuecat.com https://e.revenue.cat; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; frame-src https://accounts.google.com https://js.stripe.com https://hooks.stripe.com;">
 
     <!-- PWA Meta Tags -->
     <meta name="theme-color" content="#22d3ee" />
@@
--- a/src/main.tsx
+++ b/src/main.tsx
@@ -80,7 +80,11 @@
 // Only register service worker on web, NOT on native Capacitor
 // Service workers conflict with Capacitor's WebView and can cause white screens
-if ('serviceWorker' in navigator && !isNativePlatform()) {
+// Register the service worker only in production web builds. Native (Capacitor)
+// WebViews and development builds should NOT register a service worker to avoid
+// stale caches delivering old question payloads.
+if ('serviceWorker' in navigator && !isNativePlatform() && import.meta.env.PROD) {
   const registerSW = () => {
     // A previous PWA version cached Supabase responses for up to one day.
     // Remove that runtime cache explicitly so dynamic question/auth data can
-    // never survive a deployment or be replayed by Chrome/PWA.
+    // never survive a deployment or be replayed by Chrome/PWA.
+    // Also proactively remove any runtime caches that might contain Supabase
+    // responses (questions, auth tokens) from older workers.
     if ('caches' in window) {
-      window.caches.delete('supabase-cache').catch(() => { /* ignore */ });
+      window.caches.delete('supabase-cache').catch(() => { /* ignore */ });
+      window.caches.keys().then((keys) => {
+        keys
+          .filter((key) => /supabase|api-cache|question/i.test(key))
+          .forEach((key) => window.caches.delete(key).catch(() => { /* ignore */ }));
+      }).catch(() => { /* ignore */ });
     }
 
     navigator.serviceWorker.register('/sw.js')
